import java.util.ArrayList;
import java.util.HashMap;
import java.util.Random;
public class Battlefield{
    public Battlefield(){

    }public Integer CalculateBossAtk(){
        Random rand = new Random();
        int highestDamageMultiplier = 11;
        int randomMultiplier = rand.nextInt(highestDamageMultiplier);
        return randomMultiplier * 1000;
    }public void AskTop3Atk(partyDetaials) {
        ArrayList<Integer> attackMemberDamage = new ArrayList<Integer>();
        int attack = (partDetails.values()).sort();
        for (partyDetails.Entry<String,Object> entry : partyDetails.entrySet()){
            Object value = entry.getValue();
        }

    }public void HPAndDefLineup(){
    }public void CalculateDamage(partyMember) {
        if partyMember.type == HelaerP

    }
}
